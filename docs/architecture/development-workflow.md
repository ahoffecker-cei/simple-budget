# Development Workflow

## Local Development Setup

**Prerequisites:**
```bash
node --version  # v18+
npm --version   # v9+
dotnet --version # 8.0+
git --version   # 2.30+
```

**Initial Setup:**
```bash